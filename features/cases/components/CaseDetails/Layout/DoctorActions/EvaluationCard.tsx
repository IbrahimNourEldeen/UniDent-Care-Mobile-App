// EvaluationCard is currently disabled (same as web version — code is commented out)
// Will be enabled when evaluation feature is ready
export default function EvaluationCard() {
    return null;
}
